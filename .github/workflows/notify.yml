# .github/workflows/notify.yml in Repo-Dependency
name: Notify App Repo
on:
  push:
    branches:
      - main

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: 🚀 Send dispatch signal to Repo-App
        uses: peter-evans/repository-dispatch@v3
        with:
          token: ${{ secrets.GH_TOKEN }} # Needs a PAT with 'repo' scope
          repository: 'Carter907/security-blog'  # The main app repo
          event-type: 'dependency-updated'     # The custom type we defined in Step 1
