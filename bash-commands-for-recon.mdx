---
title: Bash Commands
date: 2025-11-09
tags:
  - cybersecurity
  - pentesting
  - knowledge-recap
  - bash
draft: true
summary: This blog goes into the depth around certain bash commands that are useful for recon or pen-testing a remote server.
---

- whoami
    - returns the current user logged into the linux session
- id
    - 
- ifconfig/ip addr
    - returns the IP address
- uname -a
    - returns all the os and machine info
- ps -ef
    - returns the current running processes?
### Information about the users

Use `cat /etc/passwd` for users

### Figure out information about their operating system

Use `cat /etc/os-release` for all the information about the system operating system.
